# Stunskin

## Installation

Install stunnel and openvpn via homebrew

Requires Full disk access for Stunskin Helper

To register the daemon, press install daemon, grant permissions as prompted in settings, and then use the button a second time.

# Stunskin

## Installation

To register the daemon, press install daemon, grant permissions as prompted in MacOS settings, and then use the button a second time.
In stunskin settings, input stunnel configuration file, openvpn configuration file, and ip/dns information as asked. 
Settings can also be setup by selecting the folder given by the stuninstall cli tool.
